<%= form_with(model: user_list) do |form| %>
  <% if user_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_list.errors.count, "error") %> prohibited this user_list from being saved:</h2>

      <ul>
        <% user_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="mb-3">
    <%= form.text_field :first_name, class:"form-control", placeholder:'First Name'  %>
  </div>

  <div class="mb-3">
    <%= form.text_field :last_name, class:"form-control", placeholder:'Last Name' %>
  </div>

  <div class="mb-3">
    <%= form.text_field :email, class:"form-control", placeholder:'Email' %>
  </div>

  <div class="mb-3">
    <%= form.text_field :phone, class:"form-control", placeholder:'Phone NUmber' %>
  </div>

  <div class="mb-3">
    <%= form.text_field :insta_id, class:"form-control", placeholder:'Insta Id' %>
  </div>
<br/>

  <div class="mb-3">
    <%= form.submit !params[:id] ? 'Create User' : 'Update User', class:'btn btn-secondary' %>
  </div>
<% end %>
